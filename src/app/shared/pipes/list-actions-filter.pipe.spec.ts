import { ListActionsFilterPipe } from './list-actions-filter.pipe';

describe('ListActionsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ListActionsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
