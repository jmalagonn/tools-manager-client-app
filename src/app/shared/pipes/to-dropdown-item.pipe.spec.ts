import { ToDropdownItemPipe } from './to-dropdown-item.pipe';

describe('ToDropdownItemPipe', () => {
  it('create an instance', () => {
    const pipe = new ToDropdownItemPipe();
    expect(pipe).toBeTruthy();
  });
});
