import { LeftPositionElementDirective } from './left-position-element.directive';

describe('LeftPositionElementDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftPositionElementDirective();
    expect(directive).toBeTruthy();
  });
});
