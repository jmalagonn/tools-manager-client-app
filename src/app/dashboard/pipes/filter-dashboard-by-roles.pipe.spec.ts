import { FilterDashboardByRolesPipe } from './filter-dashboard-by-roles.pipe';

describe('FilterDashboardByRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDashboardByRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
