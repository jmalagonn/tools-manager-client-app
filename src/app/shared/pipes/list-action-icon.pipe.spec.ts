import { ListActionIconPipe } from './list-action-icon.pipe';

describe('ListActionIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ListActionIconPipe();
    expect(pipe).toBeTruthy();
  });
});
