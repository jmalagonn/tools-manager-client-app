import { LastCharactersPipe } from './last-characters.pipe';

describe('LastCharactersPipe', () => {
  it('create an instance', () => {
    const pipe = new LastCharactersPipe();
    expect(pipe).toBeTruthy();
  });
});
