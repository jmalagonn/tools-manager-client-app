import { OutputToolStatePipe } from './output-tool-state.pipe';

describe('OutputToolStatePipe', () => {
  it('create an instance', () => {
    const pipe = new OutputToolStatePipe();
    expect(pipe).toBeTruthy();
  });
});
