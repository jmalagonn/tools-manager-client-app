import { SortWorkOrdersPipe } from './sort-work-orders.pipe';

describe('SortWorkOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new SortWorkOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
