import { WorkStatePipe } from './work-state.pipe';

describe('WorkStatePipe', () => {
  it('create an instance', () => {
    const pipe = new WorkStatePipe();
    expect(pipe).toBeTruthy();
  });
});
