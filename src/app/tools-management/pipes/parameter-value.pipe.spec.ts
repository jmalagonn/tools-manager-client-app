import { ParameterValuePipe } from './parameter-value.pipe';

describe('ParameterValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ParameterValuePipe();
    expect(pipe).toBeTruthy();
  });
});
