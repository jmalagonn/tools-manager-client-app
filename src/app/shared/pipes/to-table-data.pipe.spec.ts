import { ToTableDataPipe } from './to-table-data.pipe';

describe('ToTableDataPipe', () => {
  it('create an instance', () => {
    const pipe = new ToTableDataPipe();
    expect(pipe).toBeTruthy();
  });
});
